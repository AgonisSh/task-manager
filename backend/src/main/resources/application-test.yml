jwt:
    # For testing, use a fixed secret and short expiration times
    secret: 3z8TnaR1y9vjAPBzU0Fut927bQgoJdaMFnsBcWFV1Ck=
    expiration: 3000  # 3 Seconds for testing
    refresh-token:
        expiration: 2000  # 2 Seconds for testing

spring:
    datasource:
        url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
        driver-class-name: org.h2.Driver
        username: sa
        password:

    jpa:
        hibernate:
            ddl-auto: create-drop
            show-sql: true
        defer-datasource-initialization: true

    sql:
        init:
            mode: never
            
    flyway:
        enabled: false
